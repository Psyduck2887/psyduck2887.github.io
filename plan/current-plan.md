# 当前执行计划

`current-plan.md` 是当前执行合同与进度记录，记录本轮工程精选页重构的范围、状态和验收要求。

## 当前目标和范围

完善并发布与主博客独立的 `/engineering/` 内容空间：修复无侧栏页面的跳转报错，统一首页内容基线和分类命名，并为两个工作项目补充独立封面。本轮不增加提交接口、统计、登录或 MCP。

## 执行纪律

- 只修改 Engineering 相关文件和必要的索引过滤逻辑，不改动现有文章正文和主站导航。
- 完成验证后提交并推送到 `main`，由 GitHub Actions 部署。
- 页面内容不放个人简历信息，只保留工程事实和文章链接。

## 上下文恢复规则

任务中断后，先读取本文件和 `plan/struct.md`，再检查 `git status`，从第一个未完成阶段继续。

## 计划变更规则

新增动态接口、改变 URL 方案或修改主站导航属于范围变化，需要先确认；样式和文案微调可在本轮直接完成。

## 阶段状态

1. 结构检查：已完成。
2. Engineering 首页：已完成；分类改为“工作项目、个人项目、架构与探索”，标题与卡片区域使用同一左侧基线。
3. Engineering 详情页：已完成专属布局和九个独立路由；Workflow Mock 与长日志分析已有第一版正文，其余页面先放内容骨架。
4. 隔离：已从主站搜索、`site.json`、单页 JSON、侧栏和搜索组件中排除。
5. 路由修复：已完成；Engineering 内部导航使用原生整页跳转，主题路由已补充无侧栏保护。
6. 工作项目配图：已完成；Workflow Mock 保留 Duck Guard 风格，长日志分析已改用水彩编辑插画。
7. 本地验证：已完成；主题和 Hexo 构建成功，首页、两篇详情页、往返跳转、图片加载和索引隔离均通过。
8. Commit 与部署：已完成交付验证，提交后推送 `main` 触发 GitHub Pages 部署。

## 下一步

本轮 Engineering 独立空间交付完成，后续根据实际使用反馈继续完善专属文章。

## 测试、Review、Commit 和交付门禁

- 测试：运行 `npm run clean && npm run build`，确认首页和全部 Engineering 详情页生成。
- 真实验证：检查首页只链接 `/engineering/...`；详情页只返回 `/engineering/`；主站 JSON 不包含 Engineering 内容。
- Review：完成构建后检查改动范围和页面源代码。
- Commit：最终验证通过后提交全部 Engineering 相关改动。
- 交付门禁：只有构建成功且本地页面可访问，才算完成本轮。
