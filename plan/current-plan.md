# 当前执行计划

`current-plan.md` 是当前执行合同与进度记录，记录本轮 A、B 内容隔离调整的范围、状态和验收要求。

## 当前目标和范围

从主博客 A 删除 AI 求职工程文章，只在 Engineering B 中保留独立版本；同时把新增、修改文章前必须确认发布范围的规则写入现有项目文档。

## 执行纪律

- 只删除 A 的求职文章源文件，不删除 B 的独立版本及其仍在使用的配图。
- 完成验证后提交并推送到 `main`，由 GitHub Actions 部署。
- 不新增规则文件，双端确认规则写入现有 `plan/struct.md`。
- 不打开或控制浏览器。

## 上下文恢复规则

任务中断后，先读取本文件和 `plan/struct.md`，再检查 `git status`，从第一个未完成阶段继续。

## 计划变更规则

如果删除 A 文章会影响 B 的文件或资源引用，必须停止并重新确认；只影响 A 的索引、归档和旧文章路由时按计划执行。

## 阶段状态

1. 定位 A、B 内容与资源引用：已完成。
2. 删除 A 的 AI 求职工程文章：已完成。
3. 增加双端发布范围确认规则：已完成。
4. 构建与隔离验证：已完成；A 的旧路由、首页、归档和搜索数据均已移除该文章，B 的页面与配图保持正常。
5. Commit 与部署：验证通过，提交后推送 `main` 触发 GitHub Pages 部署。

## 下一步

本轮 A、B 内容隔离调整完成，后续新增或修改文章时先向用户确认发布范围。

## 测试、Review、Commit 和交付门禁

- 测试：按 GitHub Actions 参数重新生成站点。
- 真实验证：A 首页、归档和搜索数据不包含求职文章；B 求职页面及配图仍正常生成。
- Review：完成构建后检查改动范围和页面源代码。
- Commit：最终验证通过后提交全部 Engineering 相关改动。
- 交付门禁：只有构建成功且本地页面可访问，才算完成本轮。
